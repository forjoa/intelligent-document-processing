services:
  - type: web
    name: intelligent-document-processing
    runtime: python
    buildCommand: apt-get install -y tesseract-ocr && pip install -r requirements.txt
    startCommand: bash run.sh --no-ui
    envVars:
      - key: DATABASE_URL
        sync: false
